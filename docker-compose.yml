services:
  # Service 1: Authentication service container
  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    container_name: paper-trading-auth
    restart: always
    ports:
      - "5101:5101"
    env_file:
      - ./auth-service/.env
    environment:
      - TRADING_SERVICE_URL=http://trading-service:5102

  # Service 2: Trading service container
  trading-service:
    build:
      context: ./trading-service
      dockerfile: Dockerfile
    container_name: paper-trading-trading
    restart: always
    ports:
      - "5102:5102"
    env_file:
      - ./trading-service/.env
    environment:
      - MARKET_SERVICE_URL=http://market-service:5103

  # Service 3: Market service container (Live Price Feed & WebSocket)
  market-service:
    build:
      context: ./market-service
      dockerfile: Dockerfile
    container_name: paper-trading-market
    restart: always
    ports:
      - "5103:5103"
    env_file:
      - ./market-service/.env

  # Service 4: API Gateway container
  api-gateway:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    container_name: paper-trading-gateway
    restart: always
    ports:
      - "5100:5100"
    env_file:
      - ./api-gateway/.env
    environment:
      - AUTH_SERVICE_URL=http://auth-service:5101
      - TRADING_SERVICE_URL=http://trading-service:5102
      - MARKET_SERVICE_URL=http://market-service:5103
    depends_on:
      - auth-service
      - trading-service
      - market-service

  # Service 5: Frontend container (Commented out because Frontend is deployed on Vercel)
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #     args:
  #       - VITE_API_URL=${VITE_API_URL:-http://localhost:5100}
  #       - VITE_WS_URL=${VITE_WS_URL:-ws://localhost:5103}
  #   container_name: paper-trading-frontend
  #   restart: always
  #   ports:
  #     - "5173:5173"
  #   depends_on:
  #     - api-gateway

# Build and run the app in the background
# docker compose up --build -d

# Check running container statuses
# docker compose ps

# View live container logs
# docker compose logs -f
